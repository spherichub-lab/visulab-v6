/**
 * Re-export query keys factory for backward compatibility
 * This file maintains backward compatibility with existing imports
 */

export * from '../hooks/queries/queryKeysFactory';
export { default } from '../hooks/queries/queryKeysFactory';
